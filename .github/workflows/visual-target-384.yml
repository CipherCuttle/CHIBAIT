name: visual-target-384
on:
  pull_request:
  push:
    branches: [feat/visual-target-384x256]
jobs:
  source-and-assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm run verify
