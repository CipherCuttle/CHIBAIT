name: slopwater-v2
on:
  pull_request:
  push:
    branches: [feat/slopwater-v2]
jobs:
  simulation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test
